var age = prompt("How old are you?");
if Number(age) === "18" alert({
	"Great, lets go newb"
};
)
	else if Number(age) > 18 alert({
		"Great, Lets go boss"
	});
	else number(age) < 18 alert({
		"Well then, lets go!"
	});
